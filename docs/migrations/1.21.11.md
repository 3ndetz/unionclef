# Migration: MC 1.21.1 → 1.21.11

Tracking all compile errors when building altoclef + tungsten for MC 1.21.11.
Source on disk targets 1.21.1 (mainProject). Preprocessor generates 1.21.11 code.
Each fix uses `//#if MC >= 12111` gates — must NOT break 1.21.1 backwards compat.

## Status

Total errors: ~100
Fixed: 15 (item class gates + DrawMode)
Remaining: ~85

## 1. Entity.getPos() → ? (~40 errors) — NOT STARTED

`Entity.getPos()` removed/renamed in 1.21.11.
Need to find the replacement method name in yarn 1.21.11 mappings.

**Files affected:**
- MobDefenseChain.java (4)
- GoalRunAwayFromEntities.java (1)
- MLGBucketFallChain.java (3)
- WorldSurvivalChain.java (2)
- EntityTracker.java (7)
- BlockScanner.java (5)
- Butler.java (2)
- LookHelper.java (2)
- GestureTask.java (6)
- DefaultGoToDimensionTask.java (1)
- ThreatTable.java (4)
- AbstractObjectBlacklist.java (1)
- EntityLocateBlacklist.java (1)
- MLGBucketTask.java (7)
- MovementProgressChecker.java (1)
- Py4jEntryPoint.java (3)

## 2. Entity.getWorld() → ? (~5 errors) — NOT STARTED

- BlockScanner.java (1)
- LookHelper.java (1)
- (tungsten files already handled via srcDir)

## 3. DimensionType.ultrawarm() → ? (3 errors) — NOT STARTED

- WorldSurvivalChain.java (1)
- MLGBucketTask.java (2)

## 4. PlayerInventory fields now private (3 errors) — NOT STARTED

`PlayerInventory.main`, `.armor`, `.offHand`, `.selectedSlot` — private in 1.21.11.

- SlotHandler.java: `selectedSlot` (1)
- InventorySubTracker.java: `main`, `armor`, `offHand` (3)

## 5. ItemCooldownManager.isCoolingDown(Item) → isCoolingDown(ItemStack) (3 errors) — NOT STARTED

- MobDefenseChain.java (2)
- KillAura.java (1)
- MLGBucketFallChain.java (1)

## 6. ColorHelper.Argb removed (9 errors) — NOT STARTED

- AltoClefTickChart.java (9)

## 7. ToolItem/Equipment in SlotHandler (2 errors) — NOT STARTED

Not caught by previous gates — SlotHandler uses these classes directly.
- SlotHandler.java: `instanceof ToolItem` (1), `instanceof Equipment` (1)

## 8. Mouse/Screen API changes (4 errors) — NOT STARTED

`mouseClicked(double,double,int)` → new signature in 1.21.11.
- GameMenuTaskChain.java (4)

## 9. Client disconnect API (3 errors) — NOT STARTED

`client.disconnect()` → `client.disconnect(Text)` etc.
- GameMenuTaskChain.java (3)

## 10. Recipe API changes (4 errors) — NOT STARTED

`CraftingRecipe.getResult()`, `.getIngredients()`, `RecipeManager` types changed.
- CraftingRecipeTracker.java (4)

## 11. DrawContext API changes (4 errors) — NOT STARTED

`RenderLayer` → `RenderPipeline`, `getMatrices()` returns different type.
- DrawContextWrapper.java (4)

## 12. Misc API changes — NOT STARTED

- AltoClef.java: `Session.getAccountType()` (1)
- MobDefenseChain.java: `getEquippedItems()` (1)
- Py4jEntryPoint.java: `takeScreenshot()` signature, `getBytes()`, `getHandItems()` (4)

## 13. Item class deletions (previously gated) — DONE

- [x] SwordItem — gated in KillAura, MobDefenseChain, AbstractKillEntityTask
- [x] ToolItem — gated in DepositCommand, ToolMaterialVer
- [x] MiningToolItem — gated in MiningToolItemAccessor, ItemHelper, MineAndCollectTask
- [x] PickaxeItem — gated in ItemHelper
- [x] Equipment — gated in EquipCommand
- [x] ToolMaterials — gated in ToolMaterialVer

## 14. Tungsten DrawMode — DONE

- [x] VertexFormat.DrawMode package moved to com.mojang.blaze3d.vertex

## Open questions

- What is `Entity.getPos()` renamed to in 1.21.11 yarn? Need to check mappings.
- What replaced `ColorHelper.Argb`?
- What is the new `mouseClicked` signature?
- What replaced `PlayerInventory.main` field access?
