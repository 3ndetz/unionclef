# PvP/ranged/chase/bridge AUDIT — 2026-07-24 (repo @ c01341c, v0.58.0)

> ⛔ **PARTIAL STATUS, checked 2026-09-02 — five weeks and 35 versions old (`v0.58.0` here,
> `mod_version` is `0.93.0` now), and this session's context is pathfinding, not combat, so this
> is a narrow check, not a full sweep.** Two of the five root causes below have concrete,
> already-established evidence from other work this session that they no longer hold; the other
> three (combat-controller/aim/DIRECT-steer, all in `altoclef` combat classes) were NOT looked
> at — no claim made either way about them, and they should not be read as still-open just
> because they weren't touched.
> - **Root cause 4 (`planPlaceMoves=false` by default, so the honest WindMouse-gated placement
>   chain never runs) — FIXED, confirmed twice independently.** A live `;settings` read this
>   session (see `TODOS.md`, the stand-stuck-live entry) showed `planPlaceMoves = true` on the
>   running stand, and `TODOS.md`'s own **C5.5** (already closed `[x]`) documents this exact
>   finding — "ships OFF, nothing in the default path turns it on" — as fixed.
> - **Root cause 5, the g-cost half (`BlockNode`'s block-space search never accumulates cost —
>   constructor discards it, relaxation is `child.cost+1`, so it is greedy despite looking like
>   A*) — FIXED.** Read directly this session while checking a different question (`docs/
>   BARITONE-PORT.md`'s COST MODELS section): `BlockSpacePathFinder`'s relaxation computes
>   `tentativeCost = next.cost + edgeCost(child, world)` — parent cost plus the edge, not
>   `child.cost+1` — gated by a real improvement test (`minimumImprovement = 0.21`) before
>   committing. The OTHER half of root cause 5 (smartMoves and the capable move generator being
>   mutually exclusive) is **still true, but now low-stakes** — checked directly:
>   `BlockNode.getChildren` (`:320-354`) still returns early when `TungstenConfig.smartMoves` is
>   on, never reaching the break/place/water/ladder/slime generation code that starts at `:356`.
>   A real fix landed for a DIFFERENT bug in the same branch (`smartMovesShareTheGuard`, `:342` —
>   smart-generated moves used to skip the physics-executability gate entirely and could hand out
>   an unexecutable 3-block drop; now they're validated like every other generator's moves) but
>   that doesn't touch the mutual-exclusion finding at all. Two reasons this matters far less than
>   in 2026-07-24: `smartMoves` still defaults `false` (`TungstenConfig.java:4460`), and this
>   whole `BlockNode`/`BlockSpacePathFinder` file is the legacy planner — `FastPlanner`, primary
>   since G-0, has its own complete move set (step/diagonal/climb/drop/parkour/placeAcross/
>   pillarUp/special) with no `smartMoves` branching of any kind. This closes the full sweep of
>   this audit's five root causes — everything above is now checked, one way or the other.
> - **Root cause 1 (the two-clocks key conflict) — FIXED**, checked directly this session by
>   reading `CombatController.java` for the first time. Its own comment at `:460-468` describes
>   the exact defect in the past tense — "Previously two subsystems pressed the keys
>   independently: the `SafetySystem` stage machine (per RENDER FRAME) and `combatMove` (per
>   CLIENT TICK). Whichever ran last... won, so movement was framerate-dependent" — and the fix:
>   one resolution point (`safety` gets priority, `closeQuarters` fills in when safety doesn't
>   want the legs, `VoidGuard` vetoes, keys written exactly once). The surrounding code carries
>   real measured before/after numbers for several follow-on tuning passes on the same
>   mechanism (melee_basic death counts, `corr(controller ticks, reachMean) = +0.91`), not just
>   an assertion — this file is at least as rigorously maintained as the pathfinding side.
> - ⛔ **Root cause 2 (aimer-vs-trigger disagreement on hitbox point) — PARTIALLY addressed, and
>   the still-open half is CONFIRMED LIVE, traced end to end, not a structural risk.** The
>   angle-tolerance half is fixed: `TriggerBot`'s own class javadoc (`TriggerBot.java:14-24`)
>   describes a DIFFERENT, already-fixed bug (gating on vanilla's outline pick, which tall grass
>   blocks with no collision) and the angle gate is now a loose 40° "wild misalignment" sanity
>   check (`MAX_LOOK_ANGLE_DEG = 40.0`, `:65`) — nowhere near the ±12° the original finding names.
>   **The LOS half is still broken, confirmed by tracing the actual call chain:**
>   `SafetySystem.computeAimPrediction` (`:631-645`) calls `findBestAimPoint`, which samples
>   head/feet/side/corner points specifically BECAUSE the near-center closest point had no clean
>   LOS (`:716-750`) — this result becomes `aimYaw`/`aimPitch` (`:657-661`), read by
>   `CombatController` (`getAimYaw()`/`getAimPitch()`, `SafetySystem.java:905-906`) and fed
>   straight into `WindMouseRotation.INSTANCE.setTarget(...)` (`CombatController.java:394,419`)
>   — this is the REAL, live rotation the bot's crosshair converges to. Meanwhile
>   `TriggerBot.tick(player, target)` is called with only the raw target entity
>   (`CombatController.java:438`) — `TriggerBot.java` has no reference to `SafetySystem`
>   anywhere in the file, and independently raycasts its own LOS check straight at
>   `box.getCenter()` (`TriggerBot.java:205-207`). So: when partial cover forces the aim onto a
>   visible limb because the center is occluded, the crosshair genuinely converges there (real,
>   verified rotation), and `TriggerBot`'s own center-raycast still reports blocked — the trigger
>   never fires on a target it is actually looking at and could actually hit. This is the exact
>   "stare, doesn't click" symptom the original 2026-07-24 finding names, reproduced by a
>   completely redesigned aim system landing on the same seam. Not fixed by this session — a real
>   fix (have `TriggerBot` accept the aim point `SafetySystem` actually used, instead of
>   recomputing its own) is a behavioural change to a widely-exercised combat gate and needs a
>   stand run, not a blind commit. ⛔ Checked later the same session which specific risk makes it
>   more than a one-line change: `gateAngle` and `gateLos` are two INDEPENDENT checks that both
>   currently reference the hitbox center; patching only the LOS raycast to use the real aim
>   point while leaving the angle check pointed at center would put the two gates on different
>   points and could manufacture a NEW mismatch rather than close the old one. Both need to move
>   to the same point together — a real geometry change, not a single-line patch.
> - **Root cause 3 (three DIRECT-steer self-destructs) — ALL THREE FIXED**, each confirmed by its
>   own code comment describing the old bug in the past tense, not inferred:
>   (a) the `groundSafe` mid-air false-bail — `BlockPathWalker.java:319-324` now reads
>   `!player.isOnGround() || CombatPathfinder.isWalkable(...)`, with a comment citing the exact
>   symptom this audit names ("false-bailed the live chase at every bunny-hop apex... armed the
>   2s steer cooldown"); (b) `setTargetFast` is not dead — confirmed live at three call sites
>   (`BlockPathWalker.java:352,513,575`), with the fix comment naming the exact old defect ("RW-9,
>   dead setTargetFast"); (c) the ground-snapped feet point clipping terrain lips — the engage
>   gate in `FollowEntityTask.java:271-273` (the chase mechanism itself, not only the combat-entry
>   path this finding worried the fix was scoped to) now checks LOS to `effectiveTarget.add(0,
>   1.0, 0)` with an inline comment: "body-centre, not feet (terrain lips)". This closes out root
>   cause 3 entirely — the only remaining open items in this whole audit are the TriggerBot/
>   SafetySystem aim mismatch above (root cause 2) and the smartMoves half of root cause 5.

## LIVE STAND RESULTS (2026-07-24, mac stand, suite `pvp` with clip capture)

First full run (before fixes): 3/8 gate PASS. After F4 + F6 + scenario fixes: **7/8 gate PASS**.

| scenario | before | after | note |
|---|---|---|---|
| melee_basic | PASS | PASS | first hit ~2.5s, kill; F4 no regression |
| edge_duel | PASS | PASS | 5x5 over void, kill, 0 self-falls |
| narrow_bridge_duel | PASS | PASS | 1-wide bedwars bridge, kill, 0 self-falls |
| chase_flat | FAIL (avg 11.9, no contact) | **PASS** | F4: contact 6.7s, avg 5.4, 0 freezes |
| chase_terrain | FAIL (never, 5 freezes) | flaky (catches: 59.6s/0 freezes, or misses) | F4 helps; **F10** move-gen needed for reliability |
| ranged_moving | FAIL (1/6) | **PASS** (2-4/6) | F6: lead from position deltas |
| bridge_assault | FAIL (placed 0) | **PASS** | scenario bug: bow in hand not cobble; bridgeTo builds 15 blocks/3.7s, kill |
| allround | FAIL (0 ranged) | **PASS** | F6 + select bow before ranged: 2 ranged hits + melee kill |
| bow_flee / _hard (info) | fail | fail | **F7** kite primitive missing (flee executor owns the camera) |

Clips (pass + fail) recorded per scenario and sent to the operator's Telegram.
NOT reproduced on the stand as a gate fail: RW-1 "stands still near target" — the
flat/edge/narrow melee scenarios pass (0 freezes / 0 standstill windows); the live
symptom likely needs a harder/human-jitter scenario (stand PASS != live theme).

Delivered this session: F4 (chase, RW-9) and F6 (bow lead, RW-6) — built, deployed,
validated on the stand, committed. Remaining per the plan below: F10 (terrain
move-gen, the chase_terrain reliability blocker), F7 (kite/bow-flee), F1-F3 (combat
rework for the live RW-1 feel), F8-F9 (physical placement), F11-F12.

---


Produced by an 8-reader parallel code audit (melee, ranged, chase, bridge,
pathfinder-core, test-infra, control-surface, TODO-claim verification), every
critical/high finding adversarially re-verified against the code by two
independent skeptics. This document is the reference for the LIVE-B combat
rework, the RW-9 chase bench work, and the ranged/bridge passes. The
companion deliverable is the unified suite: `deploy/runner/run_suite.py` +
`deploy/runner/uctest/` (docs/features/PVP_SUITE.md).

## The five root causes behind the live symptoms

1. **RW-1 "stands still near the target" — two clocks fight over the same
   keys.** Combat legs are written twice: `CombatController.combatMove` at
   game-tick HEAD (the write vanilla actually samples) and the
   `SafetySystem.renderUpdate` stage machine at render frequency (~60 FPS,
   sampled never). Whenever a safety stage is active (`NARROW_BATTLE` on
   bridges, `DANGER_BATTLE`/`DANGER_IMMINENT` near edges) combatMove does
   `releaseLegs()` and returns — the render-frame stage keys are wiped one
   line earlier, so the sampled input is all-released: the bot literally
   stands, worst exactly on bedwars/edge terrain
   (CombatController.java:112-115 vs SafetySystem.java:194-285).
2. **RW-1 "stares, doesn't click" — trigger and aimer disagree on the
   point.** The aimer deliberately targets the best VISIBLE hitbox sample
   (head/feet/side) under partial cover; TriggerBot gates the attack on a
   raycast + angle to the hitbox CENTER (TriggerBot.java:67-93). With partial
   cover: aim converges, gate never opens, 100 hitless ticks later punk flips
   back to APPROACH — the observed stare loop.
3. **RW-9 "never catches live, stand PASS" — DIRECT steer self-destructs.**
   (a) the walker's per-tick `groundSafe` bail uses
   `CombatPathfinder.isWalkable(feet)`, false MID-AIR, so every bunny-hop
   bails DIRECT at the jump apex and arms the 2s steer cooldown
   (BlockPathWalker.java:229-241); (b) `setTargetFast` — the fast nav turn
   the walker's own comments rely on to keep the 45° face-before-move gate
   open — has ZERO call sites (dead code), so every bearing change stalls
   sprint behind the slow humanized turn (BlockPathWalker.java:253, 324);
   (c) the steer engage gate raycasts to the target's ground-snapped FEET
   point, which clips terrain lips (fix already known and applied only to
   combat entry, PunkPlayerTask), so DIRECT rarely engages off flat ground
   (FollowEntityTask.java:217-219). Stand tests could not catch any of this:
   every "moving target" was rcon-teleported (zero velocity, 2s stationary
   dwell) on a flat floored arena with permanent LOS.
4. **RW-2/RW-3 "cheaty building" — two placement stacks, the honest one is
   default-off.** The core place-as-a-move chain (PathExecutor.tickPlacing)
   is WindMouse-aim-gated, one block per timing window — and OFF
   (planPlaceMoves=false). Everything visible in the demos uses the cheaty
   stack: godbridge `interactBlock`s the SAME tick it sets the aim target
   (camera is cosmetic) with NO use-cooldown (up to 20 places/s,
   BridgeTask.java:179-185); WorldEdit/fill/build ops funnel into
   `placeBlockAtRaw` = one-shot full-delta camera snap + instant place,
   batched up to 96 cells inside ONE client-thread call — "all 6 glass at
   once" confirmed (Py4jEntryPoint.java:2702-2738, 1861-1867).
5. **#67 "Ran out of nodes" — smartMoves and the capable move-gen are
   mutually exclusive.** `BlockNode.getChildren` returns early under
   smartMoves, never reaching the ONLY branch with break-through,
   place/bridge, water, ladder, slime move-gen (BlockNode.java:292-301); and
   the block-space "A*" never accumulates g-cost (constructor discards move
   cost; relaxation uses `child.cost+1`) — it is a greedy search, all
   SmartMoves costs are dead (BlockNode.java:168, BlockSpacePathFinder.java:275).

## Verified findings by subsystem (critical/high, all CONFIRMED by 2 skeptics)

### Melee (tungsten punk)
- CombatController.java:112 — stage movement neutralized by releaseLegs (root 1). CRITICAL.
- CombatController.java:138-142 — 2.0–3.4 band disables fwd/back/sprint; walk-strafe void-gated per side → hop-in-place on narrow ground.
- TriggerBot.java:74 — LOS gate to center vs aim at visible point (root 2).
- CombatController.java:120-131 — no-LOS + BFS path<2 ⇒ legs released AND no aim target set; camera stale-releases after 600ms = full freeze.
- VoidGuard.java:54-63 — cancels ALL keys + forces sneak when the resultant heading points at a drop; kills rim-parallel strafing (should clamp only the offending component).
- Notable rest: ESCAPE stage unreachable; combatExecutorEnabled never read (30-tick sim burns CPU for viz); zero weapon handling in punk pipeline (no axe/shield/mace; altoclef bestWeapon lives in the legacy path punk doesn't use); braking/repositioning aims camera at horizon (zero attacks in danger stages); wall-clock+Math.random cadences (non-deterministic).

### Ranged
- TrajectorySolver.java:43 — tungsten BowShooter leads off `getVelocity()` ≈ 0 for remote players (the repo documents this itself); only the altoclef path got the position-delta fix. The py4j `shootArrowAt` primitive has NO lead on real players.
- RunAwayTask.java:69 — no shoot-while-fleeing anywhere; every shot is taken standing still; flee executor owns the camera (no WindMouse arbiter) → bow-flee is architecturally impossible today.
- ShootArrowSimpleProjectileTask.java:321 — altoclef release is a pure charge timer, no aim-convergence gate (BowShooter requires 3.5°).
- ShootArrowSimpleProjectileTask.java:263 — rapid-fire releases partial charge while the solver always assumes full-charge speed 3.0 → systematic low shots inside ~20 blocks.
- Rest: KillPlayerTask ranged = 1 arrow/4-10s, branch annotated "TODO NOW UNTESTED"; artillery still legacy g=0.006 closed-form; no trident/snowball/egg primitives; @punk is melee-only.

### Chase / follow / flee
- BlockPathWalker.java:229 — groundSafe false mid-air: bunny-hop self-bails DIRECT every hop + 2s cooldown (root 3a). CRITICAL.
- BlockPathWalker.java:253/324 — setTargetFast dead code (root 3b).
- FollowEntityTask.java:217-219 — feet-point LOS engage gate (root 3c).
- PathFinder.java:120 — physics fallback loses 0.5-3.5s per engagement (500ms startup sleep, frozen-snapshot goal, 0.8-block drift stop, momentum dropped at handoffs).
- follow_altoclef_test.py:159 — the "validating" stand test tp-hops the victim (quasi-static, flat, permanent LOS) — cannot falsify live behavior.
- Rest: BFS walker gated to dist>6 → close-range no-LOS dead zone; RunAwayTask 100% physics-driven with a no-op re-plan race; gap-jumps unreachable outside the executor (tickBFS jump gate scans the gap as unsafe landing); FollowEntityTask leaks searchTimeoutMs into the GLOBAL config (PathFinder.java reads it for every search; only GotoCommand restores).

### Bridge / build
- BridgeTask.java:179-185 — godbridge zero aim gate + zero use-cooldown (root 4). Fix point for RW-3.
- Py4jEntryPoint.java:2702-2738 — fillCells: up to 96 placements in ONE tick (RW-2 confirmed); same pattern replaceStatus/buildBlocks/buildDefenseAround.
- Py4jEntryPoint.java:1861-1867 — placeBlockAtRaw full-delta one-shot camera snap + instant place (flaggable).
- PathFinder.java:257-263 — core_bridge 2/6 flakiness root CONFIRMED in code comments: block search returns a fall-partial with no place plan → physics leg sims across the un-bridged gap and falls; correct fix = physics search completes at the truncated endpoint when places/breaks are pending.
- Rest: bridgeTo is one cardinal leg, computed once, target-Y ignored (cannot chase a moving island target); search-level bridge move same-Y/cardinal/strictly-goal-closer; shredder jump-bridge machine snap-rotates + reflection-resets itemUseCooldown (flaggable set); PathExecutor.tickPlacing/tickBreaking is the aim-gated humanized model everything should converge on.

### Pathfinder core
- BlockNode.java:292 — smartMoves bypasses break/place/water/ladder/slime move-gen (root 5). CRITICAL.
- BlockNode.java:168 — g-cost never accumulated; greedy search; SmartMoves costs dead. CRITICAL.
- PathFinder.java:108 — no goal-retarget: find() silently no-ops while active; chase re-plan = stop+wait+restart with 2s/3-block hysteresis.
- FollowEntityTask.java:186 — hold-position early-return inside closeEnough; enableLeap default false; <6-block search budget 120ms behind 500ms overheads → close-range dead zone.
- BlockSpacePathFinder.java:143-148 — budget applies only once `failing` clears (5 blocks progress) → unbounded CPU spins on open terrain.
- BlockPathWalker.java:313/338 — waypoint tolerance 1.5 + 45° sprint gate vs 1-wide bridges (corner-cut falls); NO void/edge cost in any search.
- Rest: TungstenHelper reflection bridge dead (silent no-op fallback); minPathSizeForTimeout dead knob; static search state races.

### Test infra (why the suite was rebuilt)
- 56 scripts × private harness copies (~6300 duplicated lines): py4j snippet in all 56, rcon in 52 (error-swallowing in ~40), wait_for in 26, arenas inline in 46. RW-5 confirmed.
- autotest.sh runs ONLY slime_test; ~23 scripts never exit non-zero (cannot gate), incl. bedwars_combat, runaway, terrain.
- Victims rcon-teleported in every chase/follow/bow test (zero velocity) — the "stand PASS ≠ live" mechanism.
- Cross-test poisoning: tungsten.json persists every ;settings write across restarts (no reset-to-defaults lever); break_test leaves driftThreshold 1.5 forever; camelCase gamerules silently rejected on 1.21.11 through the error-swallowing rcon.
- Capture tooling (x11grab, victim-POV, screenshots, noVNC) exists but is disconnected from tests; no ranged demo (RW-6).

### Control surface (levers) — gaps that block assertions
- No kite/bow-flee lever (CRITICAL for the bow-flee scenario).
- No in-band fight telemetry: punkStatus/runAwayStatus = {active,target} only; no followStatus, no re-plan/bail counters, no swing/hit events (CRITICAL for "never pauses / never re-routes" assertions; suite v1 falls back to 1s rcon sampling).
- No bowStatus (BowShooter.isActive/getShotsFired unexposed).
- No determinism levers (unseeded WindMouse Random, wall-clock cadences).
- combatBunnyHop* are `long` fields — SettingsCommand only generates setters for bool/double/float/int ⇒ NOT settable via ;settings (the v0.49 "live-tunable" claim is half-true).

## TODO-claim verification (12 claims from TODOS.md "[x] FIXED" entries)

CONFIRMED (9): LIVE-A v0.52 follow rewire (steerLive, ~4-block rolling guard,
bail cooldown, tickDirect containment); TriggerBot gates (reach 3.0 / 40° /
cooldown 0.95, 0.85 crit / COLLIDER LOS + per-swing latch); LIVE-D sneak fix
(IMPLEMENTED — TODOS "[~] in progress" marker is stale, flip to [x]);
defaults (followBlockPathFinderEnabled=true, enableLeap=false,
combatMovementsEnabled=true, primary=false, planPlaceMoves=false,
smartMoves=false; setTungstenPathing couples smartMoves); core bridge chain;
RunAwayTask; TrajectorySolver + altoclef wiring; v0.28 void clamp; 9702cac
partial-path (but "furthest-progress" selection only under smartMoves).

PARTIAL (3): combatBunnyHop* not actually ;settings-tunable (long fields);
MCP has 54 tools not ~24 (benign); @shoot has rapid-fire + artillery but NO
"sniper" mode by any name and no user-selectable angle type (README/TODOS
overstate).

## Ordered fix plan (each fix gated by a named suite scenario)

| # | Fix | Where | Gated by |
|---|-----|-------|----------|
| F1 | ONE legs driver: stage machine computes state only, combatMove executes stage intent per tick | CombatController.java:112, SafetySystem.java:194-374 | melee_basic, edge_duel, narrow_bridge_duel |
| F2 | Trigger gates on the aimer's chosen point (raycast + angle to aim point) | TriggerBot.java:67-93 | melee_basic (stare loop) |
| F3 | Continuous orbit in reach band + VoidGuard component-projection clamp (cancel only the offending axis) | CombatController.java:138, VoidGuard.java:54 | edge_duel, narrow_bridge_duel |
| F4 | Chase: skip groundSafe bail while airborne; wire setTargetFast in the walker; steer LOS to body-center | BlockPathWalker.java:229/253/324, FollowEntityTask.java:217 | chase_flat, chase_terrain |
| F5 | Follow physics leg: drop 500ms sleep, re-target goal to live position at emit; per-find timeout param instead of global mutation | PathFinder.java:108-130, FollowEntityTask.java:300 | chase_terrain |
| F6 | Ranged: position-delta velocity tracker in BowShooter; aim-convergence release gate + charge-aware solve in ShootArrow task | TrajectorySolver.java:43, ShootArrowSimpleProjectileTask.java:263/321 | ranged_moving, allround |
| F7 | KITE primitive (flee + turn-shoot-turn) + WindMouse aim arbiter + bowStatus lever | new tungsten task; Py4jEntryPoint | bow_flee promotion to gate |
| F8 | Godbridge: aim-convergence gate + ≥4-tick jittered use cooldown (tickPlacing model) | BridgeTask.java:179-185 | bridge_assault placement-rate flag |
| F9 | Builds through an executor-owned queue (one aim-gated place per timing window; walk-to-cell when out of reach) | Py4jEntryPoint.java:2702/1861 → PathExecutor.tickPlacing model | RW-2 build scenarios (next suite) |
| F10 | SmartMoves parity (break/place/water/ladder moves) + honest g-cost accumulation, A-course canary green each step | BlockNode.java:292/168, BlockSpacePathFinder.java:275 | chase_terrain, #67 gamer_smoke |
| F11 | Telemetry levers: combatTelemetry/chaseTelemetry ring, followStatus, bowStatus; ;settings resetDefaults; WindMouse seed / testDeterministic | Py4jEntryPoint, TungstenConfig, WindMouseRotation.java:55 | tightens every suite assertion |
| F12 | Migrate legacy runner scripts onto uctest lib; exit codes everywhere; fold demos (capture) into scenarios | deploy/runner/* | RW-5 completion |

Suggested order: F1→F2→F3 (one combat pass, LIVE-B), F4→F5 (one chase pass,
RW-9), F6 (ranged pass), F8 (quick RW-3 win), F7, F10 (deep, own pass), F9,
F11 alongside whichever pass needs the assertion, F12 continuous.

Every fix lands only after `run_suite.py pvp` on the stand (Mac) — RW-4:
real server + anti-cheat check for every mechanic change remains mandatory
on top of the stand.
