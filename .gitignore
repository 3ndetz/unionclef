# Gradle
.gradle/
build/
versions/*/build/
versions/*/bin/
*/bin/
*/.classpath
.classpath

# IDE
.idea/
*.iml

# OS
.DS_Store
Thumbs.db

# Java
*.class
*.jar
*.war

# Runtime
run/
logs/

# Python
__pycache__/
.lock
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
.python-version

# Script output
scripts/result.txt
scripts/custom/result.txt
shredder/.classpath

# Autonomous-mode runtime state (Stop hook) — local session state, not repo content
.claude/autonomy_active.flag
.claude/autonomy_stop.flag
.claude/.autonomy_block_count
deploy/runner/artifacts/
*.srchash

# per-machine: the GPU probe wrote down that a client cannot render here
deploy/.gpu_unusable
ab*.log

# Agent memory mounted into the workspace -- one original lives outside this repo.
memory/

# Test-run debris — regenerated by the stand, never shipped (cleaned 2026-09-10)
deploy/runner/freezes/
deploy/runner/gamer_spawn.txt
deploy/runner/gamer_run_index.txt
deploy/runner/gamer_arm_index.txt
deploy/runner/gamer_pair_spawn.txt
deploy/runner/gamer_pair_spawn.txt.logs
docs/traces/
docker.sock

# iron-stall capture log (regenerated)
deploy/runner/iron_stall_log.txt
